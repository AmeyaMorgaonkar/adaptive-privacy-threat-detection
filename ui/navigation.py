# This module has been merged into ui/app.py (Sidebar + Header classes).
# Kept as a stub to avoid import errors from any remaining references.

from ui.app import Sidebar, Header

__all__ = ["Sidebar", "Header"]
